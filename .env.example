# The HTTP server binds to loopback by default because Finora has no auth layer.
FINORA_HOST=127.0.0.1
FINORA_PORT=3011

# Defaults to ~/.finora/finora.db.
# FINORA_DATA_DIR=/absolute/path
# FINORA_DATABASE_PATH=/absolute/path/finora.db
